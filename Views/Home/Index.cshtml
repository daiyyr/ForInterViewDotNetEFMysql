@using MvcApplication1.Models

@{
    ViewBag.Title = "Index";
}

<h2 style="text-align:center">Student</h2>

<!--Razor引擎-->
@Html.Raw(ViewBag.htmlStr)

<html>
<head>
    <script type="text/javascript">
        function del(id) {
            if (confirm("Are you sure to delete?")) {
                window.location = "/home/del/" + id;
            }
        }
    </script>
    <style type="text/css">
        #tblist {
            border: 1px solid #0094ff;
            width: 600px;
            margin: 10px auto;
            border-collapse: collapse;
        }

            #tblist th, td {
                border: 1px solid #0094ff;
                padding: 10px;
                text-align:center;
            }
    </style>
</head>
<body>
<table id="tblist">
    <tr>
        <th>id</th>
        <th>name</th>
        <th>dob</th>
        <th>class</th>
        <th>action</th>
    </tr>
    @foreach (student stu in ViewData["DataList"] as List<student>)
{
        <tr>
            <td>@stu.id</td>
            <td>@stu.name</td>
            <td>@stu.dob</td>
            <td>@stu.sxc.Select(r => r.@class.name).ToList()[0]</td>
            <td>
                <a href="javascript:del(@stu.id)">delete</a>
                <a href="/home/modify/@stu.id">modify</a>
            </td>
        </tr>
}

</table>
</body>
</html>